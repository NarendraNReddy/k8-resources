kind: Pod
apiVersion: v1
metaata:
  name: pod-config
spec:
  containers:
    - name: nginx
      image: nginx
      env:
      - name: "course"
        valueFrom: 
          configMapKeyRef:
            name:  config-map
            
      - name:  "duration"
        valueFrom: 
          configMapKeyRef:
            name:  config-map             
          

          


